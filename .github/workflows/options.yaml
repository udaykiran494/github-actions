name: Job with Timeout

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5        # ← entire job times out after 5 minutes

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run your step
        timeout-minutes: 5    # ← individual step timeout (optional)
        run: |
          echo "This step will be killed if it runs longer than 5 minutes"
          # your commands here